# 更新记录

本项目遵循[语义化版本](https://semver.org/lang/zh-CN/)。当前处于 `0.x` 开发阶段，版本标签统一使用 `vX.Y.Z`。

## 未发布

暂无。

## 0.1.1 - 2026-09-12

### 新增

- 新增项目搜索与回收站，支持移入回收站、撤销、恢复，以及二次确认后的永久删除。
- 新增从“创建项目”到“结果导出”的统一七阶段导航，创建前后共享同一套流程心智模型。
- 新增独立产品首页、独立项目库和独立新建项目页；项目流程的六个业务阶段均有可刷新、可分享的稳定地址。
- 数据概览增加画像指标、读取警告和前五行原始数据预览。
- 新增可重复执行的 README 产品截图脚本，使用同一个脱敏演示项目生成首页、流程、创建、诊断和结果页面截图。

### 优化

- 首页重构为整屏产品叙事：首屏使用与数据工作相关的真实感图片，下一屏使用五阶段交互流程解释处理逻辑。
- 产品流程仅在指针位于流程操作台时切换步骤，页面滚动与流程滚动互不干扰；首屏与流程屏之间按完整视口切换。
- 五个流程阶段采用独立视觉表达；移除遮挡内容的结果浮层，保证规则选项与数据细节完整可见。
- 工作台改为紧凑项目台账，修复侧栏遮挡，移除没有独立去向的“最近项目”和“系统状态”伪导航。
- 新建项目与项目详情统一标题、间距、状态反馈和步骤关系；上传成功后自动进入新项目的数据概览。
- Better Data 品牌入口在工作台和项目页中统一返回产品首页。
- 提前说明 XLSX 与聚类模式的预览边界，并在不可执行阶段提供明确原因。
- 补齐键盘焦点、减少动效、窄屏布局、进行中状态和可见成功反馈。

### 修复

- 修复删除确认对话框因菜单卸载而丢失项目信息，项目名不再显示为 `undefined`。
- 修复 Windows 下目录被短暂占用时移入回收站失败；增加同卷目录移动回退，并保持冲突保护。
- 修复项目移动后列表仍显示旧状态的问题，回收、恢复和永久删除后均重新读取后端真实状态。
- 修复第二次导入非 UTF-8 CSV 可能创建失败项目的问题；CSV 读取新增 UTF-8、UTF-8 BOM、GB18030 与 Windows-1252 编码回退。
- 修复上传早期失败后的临时目录残留，避免项目库出现无法继续的半成品。
- 修复首页切屏出现半页残留及产品流程内部滚动带动页面的问题。
- 修复流程视觉中的结果标签覆盖第三步“采用 / 不采用”等主体内容的问题。

### 工程

- 将 `backend/work/` 和本地项目数据从版本控制中排除，README 截图只使用仓库内合成数据。
- 补齐 PR #25、PR #28 及本次发布的开发记录、验证证据和已知限制。
- 新增标准发布流程、发布 Issue 模板和更完整的 Pull Request 质量门禁。
- 前端 ESLint、生产构建、后端 34 项自动化测试和 Git 差异检查作为发布前置条件。

## 0.1.0 - 2026-09-11

### 新增

- 建立 Better Data 本地数据工作台的首个可运行界面。
- 冻结 v1.0 产品需求、系统架构、开发路线图和本地优先架构决策。
- 使用 UI/UX Pro Max 生成并固化 Better Data 设计系统，增加可追溯的原始建议与项目级 `uidesign.md`。
- 增加仓库导览、文档索引以及前端、后端和通用组件目录说明。
- 完成 CSV 流式导入、基础数据画像、抽样标记、五行预览和常见分隔符识别。
- 增加可持久化项目库设置、首次运行确认、项目库身份标记和单项目详情 API。
- 增加 12 种可编辑字段角色、六维质量评分、稳定规则编号和可持久化建议选择。
- 增加分类、回归和纯数据清洗流水线，遵循训练集拟合、测试集转换的数据边界。
- 增加固定种子划分、缺失值处理、One-Hot、未知类别、缩放和常量筛选。
- 增加 Parquet 工作文件、JSON 学习状态、输入与结果哈希以及上游变更失效机制。
- 增加逻辑回归与 Ridge 快速基线比较，并强制复用处理前后的同一训练测试划分。
- 增加 UTF-8 BOM 结果 CSV、无外链离线 HTML 报告和固定白名单下载接口。
- 建立 GitHub Issues、Milestones、Pull Requests、Actions 和 Releases 的协作记录。

### 验证

- 后端自动化测试基线达到 29 项。
- 前端 ESLint、生产构建和 GitHub Actions 全部通过。
