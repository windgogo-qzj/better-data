# 参与开发

Better Data 采用中文 Issue、Pull Request 和提交说明，代码标识符保持英文。

## 工作流

1. 从 `main` 创建短生命周期功能分支。
2. 一个 Issue 对应一个可验证的目标。
3. 提交应保持单一目的，并说明行为变化。
4. Pull Request 必须关联 Issue，列出验证方式和已知限制。
5. 前端构建、后端测试和基础安全检查通过后才能合并。

## 数据安全

不得提交真实用户数据、项目库、Parquet 工作副本、导出结果、访问令牌、密钥、包含字段样例的日志或无法确认许可证的数据集。测试数据应为人工合成或明确允许再分发的数据。
