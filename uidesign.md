# Better Data 界面设计系统

> 版本：1.0  
> 适用范围：Better Data 本地数据预处理工作台  
> 设计来源：UI/UX Pro Max 的 `Data-Dense Dashboard` 建议，并结合“完全离线、中文优先、分析人员工作台”的产品约束整理。  
> 优先级：本文件是当前前端实现的唯一设计基线；`design-system/better-data/MASTER.md` 保留生成器的原始建议供追溯。

## 1. 产品与视觉方向

- **产品类型**：本地优先的数据预处理与特征工程生产力工具。
- **页面类型**：数据密集型工作台，而非营销落地页。
- **用户目标**：尽快创建项目、理解系统状态、继续最近任务。
- **视觉性格**：可信、精确、克制；使用深蓝表达可靠性，琥珀色提示关键注意事项。
- **设计参数**：视觉变化 5/10、动效 3/10、信息密度 8/10。
- **页面主操作**：每个视图只保留一个高强调主操作；首页为“新建项目”。
- **禁用模式**：不使用装饰性图片、渐变背景、玻璃拟态、Emoji 图标、无意义动效和布局位移式悬停。

## 2. 颜色系统

组件只能使用语义色变量，不在组件中散落十六进制颜色。

| 语义 | Token | 色值 | 用途 |
| --- | --- | --- | --- |
| 页面背景 | `--background` | `#F8FAFC` | 应用底色 |
| 主文字 | `--foreground` | `#172554` | 标题、重要正文 |
| 卡片 | `--card` | `#FFFFFF` | 内容表面 |
| 次级表面 | `--secondary` | `#EFF6FF` | 分组、空状态、选项背景 |
| 弱化表面 | `--muted` | `#E9EEF6` | 禁用区、占位区 |
| 次级文字 | `--muted-foreground` | `#475569` | 说明、元数据；正常字号下保持 4.5:1 |
| 主色 | `--primary` | `#1E40AF` | 主按钮、选择状态、链接 |
| 主色文字 | `--primary-foreground` | `#FFFFFF` | 主色表面文字 |
| 强调色 | `--accent` | `#D97706` | 关键提示、待关注状态，不大面积铺底 |
| 强调色文字 | `--accent-foreground` | `#1C1917` | 强调色浅背景上的文字 |
| 边框 | `--border` | `#CBD5E1` | 输入框、卡片、分隔线 |
| 焦点 | `--ring` | `#2563EB` | 键盘焦点轮廓 |
| 成功 | `--success` | `#047857` | 完成、在线、可用；必须同时有文字或图标 |
| 警告 | `--warning` | `#B45309` | 风险、待确认；必须同时有文字或图标 |
| 错误 | `--destructive` | `#B91C1C` | 错误、破坏性操作 |
| 深色导航 | `--sidebar` | `#0F1F3D` | 桌面侧栏 |
| 深色导航文字 | `--sidebar-foreground` | `#E2E8F0` | 侧栏正文 |

状态不能只依赖颜色：成功、警告、错误均搭配图标与明确文本。普通文字对比度至少 4.5:1，组件边界与焦点指示至少 3:1。

## 3. 字体与数字

项目必须完全离线，因此不请求 Google Fonts。字体栈如下：

- **界面与标题**：`"Segoe UI", "Microsoft YaHei UI", "PingFang SC", system-ui, sans-serif`
- **代码、编号与数据**：`"Fira Code", "Cascadia Code", Consolas, monospace`；Fira Code 不存在时自动回退。
- **正文**：16px / 1.625，字重 400。
- **标签**：14px / 1.5，字重 500；12–13px 只用于文件大小、时间、编号等元数据。
- **标题层级**：H1 30/36、H2 18/28、H3 16/24；桌面 H1 可到 36/44。
- **数字**：指标、文件大小、时间使用 `font-variant-numeric: tabular-nums`。
- **行长**：说明文字桌面不超过 70 个字符，移动端保持自然换行；文件名和 ID 允许 `overflow-wrap: anywhere`。

## 4. 间距、尺寸与层级

采用 4/8 像素节奏：

| Token | 值 | 用途 |
| --- | --- | --- |
| `--space-1` | 4px | 图标内部、紧凑间距 |
| `--space-2` | 8px | 同组控件间距 |
| `--space-3` | 12px | 紧凑内边距 |
| `--space-4` | 16px | 默认组件内边距 |
| `--space-6` | 24px | 卡片与小节间距 |
| `--space-8` | 32px | 页面段落间距 |
| `--space-12` | 48px | 大区块间距 |

- **页面最大宽度**：1440px；内容区随侧栏剩余空间自适应。
- **页面边距**：手机 16px、平板 24px、桌面 32px。
- **控件高度**：桌面至少 40px；触控场景与主操作至少 44px。
- **圆角**：输入与按钮 8px，紧凑卡片 12px，大容器与对话框 16px；不混用超大胶囊圆角。
- **阴影**：卡片默认无阴影，以边框分层；浮层使用 `0 16px 40px rgb(15 31 61 / 0.16)`。
- **Z 轴**：内容 0、粘性导航 20、侧栏 30、对话框 50、通知 60。

## 5. 组件规则

### 按钮

- 主按钮使用主色实底；次按钮使用白底边框；弱操作使用 Ghost。
- 必须提供默认、Hover、Pressed、Focus-visible、Disabled、Loading 六种状态。
- Hover/Pressed 只改变颜色、阴影或透明度，不移动布局。
- 异步提交时禁用重复点击，显示加载图标，并通过 `aria-busy` 表达状态。
- 仅图标按钮必须有 `aria-label`；图标与可见文本重复时图标设为 `aria-hidden="true"`。

### 卡片与指标

- 卡片使用白色表面、1px 边框和 12–16px 圆角。
- 只有可点击卡片显示 Hover/Pressed；静态卡片不得出现可点击暗示。
- 指标由图标、标签、数值、解释组成；数值与解释在放大文字时允许换行。

### 文件上传

- 拖放只是增强能力，必须同时提供可点击、可键盘操作的文件选择按钮。
- 上传区需说明格式、大小限制与“数据只在本机处理”。
- 拖入状态使用边框、背景、图标和文本共同反馈。
- 文件错误在控件下方显示，并使用 `role="alert"` 与 `aria-describedby` 关联。

### 任务类型选择

- 使用语义化 `fieldset` 与 `legend`。
- 单选状态用 `aria-pressed` 表达；选中状态同时使用勾选图标、边框和文本。
- 选择按钮不得小于 44px，并在 375px 下保持双列可读；空间不足时单列。

### 对话框

- 使用现有 shadcn/Base UI Dialog，保留自动焦点管理、焦点陷阱和 Esc 关闭。
- 必须含标题、描述和明确关闭按钮；操作区始终在内容之后。
- 手机端距视口至少 16px，最大高度不超过 `calc(100dvh - 32px)`，内容可滚动。
- 验证失败时错误出现在表单字段附近，并由屏幕阅读器播报；焦点不能被粘性操作栏遮挡。

### 导航与状态

- ≥1024px 使用左侧栏；<1024px 使用顶部品牌栏和最多 5 项的底部主导航。
- 当前页面需同时通过图标、文字权重与背景表达，不能只改颜色。
- 暂未实现的目的地显示“即将开放”或禁用状态，不提供失效链接。
- 固定底栏必须为页面内容预留底部空间并考虑 `env(safe-area-inset-bottom)`。

## 6. 响应式布局

| 视口 | 规则 |
| --- | --- |
| `< 640px` | 单列；16px 页面边距；指标横向紧凑排列后纵向堆叠；对话框近全宽；底部导航固定；避免横向滚动 |
| `640–767px` | 指标可三列；任务步骤保持三列或根据文本自动换行 |
| `768–1023px` | 24px 页面边距；主内容与辅助信息仍可单列；底部导航保留 |
| `1024–1279px` | 显示 240px 侧栏；隐藏底部导航；主区优先单列，辅助卡片双列 |
| `≥1280px` | 上传工作区与 336px 辅助栏组成双列；32px 页面边距 |

验收视口固定为 375、768、1024、1440px，并补测 375px 横屏。放大至 200% 时不得出现功能性横向滚动、文字裁切或被固定元素遮挡。

## 7. 动效

- 交互反馈使用 150ms，浮层使用 200ms；标准缓动 `cubic-bezier(0.2, 0, 0, 1)`。
- 动画只使用 `opacity` 与 `transform`，且不依赖动画完成业务状态。
- 页面不使用滚动揭示动画；该工具优先即时可读与低干扰。
- `prefers-reduced-motion: reduce` 时关闭非必要动画、平滑滚动和旋转之外的过渡；加载旋转保留但缩短视觉负担。

## 8. 可访问性基线

- 页面提供“跳到主要内容”链接，标题层级连续，主内容使用唯一 `main`。
- 所有功能可仅用键盘完成；Tab 顺序与视觉顺序一致。
- 所有交互元素使用 2px 高对比 `focus-visible` 轮廓并留 2px 偏移。
- 粘性顶部栏和底部栏不能遮挡聚焦元素；主滚动容器设置对应的 `scroll-padding`。
- 图标统一使用 Lucide 线性图标；装饰图标隐藏，图标按钮提供可访问名称。
- 状态更新使用单一、克制的 `aria-live` 区域；错误使用 `role="alert"`。
- 不禁止页面缩放；支持系统大字体与 `prefers-reduced-motion`。
- 无登录、验证码或自动轮播，因此认证与轮播条款不适用。

## 9. 内容规范

- 文案使用简体中文，产品名、文件格式、技术标识符保留英文。
- 操作文字使用“动词 + 对象”，如“新建项目”“选择文件”“创建并检查”。
- 错误信息必须包含原因和下一步，例如“CSV 文件超过 1 GB，请压缩数据或拆分后重试”。
- 不使用“成功/失败”之外的模糊反馈；长期任务说明其仍在本机后台继续。

## 10. 交付检查表

- [ ] 颜色、字体、间距和圆角全部来自本文件定义的 Token。
- [ ] 主按钮只有一个；所有交互状态完整且不引发布局位移。
- [ ] 375、768、1024、1440px 无横向滚动或遮挡。
- [ ] 上传、对话框、任务选择与底部导航可用键盘完整操作。
- [ ] 正常文字对比度 ≥4.5:1，焦点和组件边界 ≥3:1。
- [ ] 装饰图标已隐藏，图标按钮具备可访问名称。
- [ ] 错误与状态不只依赖颜色，并可被辅助技术感知。
- [ ] `prefers-reduced-motion` 生效；200% 缩放保持可用。
- [ ] 不发起字体、图片、分析或其他外部网络请求。
